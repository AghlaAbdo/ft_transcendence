services:
  nginx:
    build:
      target: base

  nextjs:
    build:
      target: production

  # game-service:
  #   build:
  #     target: production
  #   env_file:
  #     ./services/game-service/.env

  # user-service:
  #   build:
  #     target: production
  #   env_file:
  #     ./services/user-service/.env

  chat-service:
    chat-service:
    build:
      target: production